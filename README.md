https://chocolatey.org/install

    choco install yarn 
    choco install node-lts
    yarn 
    yarn dev

    ====
    node portable (add path)
    npm i -S yarn (add path)
    ====
    yarn 
    yarn dev
